extends layout

block content
  h1 Most recent Posts
  each post in posts
    div.post.center
      h3 #{post.title}
      h3 #{post.url}
      p #{post.content}
      form(method="post", action="/api/deletePost/" + post.id)
        button(type="submit") Delete
      
